builds:
-
  goos:
    - linux
  goarch:
    - amd64
  env:
    - CGO_ENABLED=0
  main: ./main.go

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
